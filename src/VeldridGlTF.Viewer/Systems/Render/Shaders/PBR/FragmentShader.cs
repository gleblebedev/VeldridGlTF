// ------------------------------------------------------------------------------
// <auto-generated>
//     This code was generated by a tool.
//     Runtime Version: 16.0.0.0
//  
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
// </auto-generated>
// ------------------------------------------------------------------------------
namespace VeldridGlTF.Viewer.Systems.Render.Shaders.PBR
{
    using System.Linq;
    using System.Text;
    using System.Collections.Generic;
    using System;
    
    /// <summary>
    /// Class to produce the template output
    /// </summary>
    
    #line 1 "E:\MyWork\VeldridGlTF\src\VeldridGlTF.Viewer\Systems\Render\Shaders\PBR\FragmentShader.tt"
    [global::System.CodeDom.Compiler.GeneratedCodeAttribute("Microsoft.VisualStudio.TextTemplating", "16.0.0.0")]
    public partial class FragmentShader : FragmentShaderBase
    {
#line hidden
        /// <summary>
        /// Create the template output
        /// </summary>
        public virtual string TransformText()
        {
            this.Write(@"#version 450

//
// This fragment shader defines a reference implementation for Physically Based Shading of
// a microfacet surface material defined by a glTF model.
//
// References:
// [1] Real Shading in Unreal Engine 4
//     http://blog.selfshadow.com/publications/s2013-shading-course/karis/s2013_pbs_epic_notes_v2.pdf
// [2] Physically Based Shading at Disney
//     http://blog.selfshadow.com/publications/s2012-shading-course/burley/s2012_pbs_disney_brdf_notes_v3.pdf
// [3] README.md - Environment Maps
//     https://github.com/KhronosGroup/glTF-WebGL-PBR/#environment-maps
// [4] ""An Inexpensive BRDF Model for Physically based Rendering"" by Christophe Schlick
//     https://www.cs.virginia.edu/~jdl/bib/appearance/analytic%20models/schlick94b.pdf

");
            
            #line 22 "E:\MyWork\VeldridGlTF\src\VeldridGlTF.Viewer\Systems\Render\Shaders\PBR\FragmentShader.tt"

	WriteDefines();

            
            #line default
            #line hidden
            this.Write("\r\n");
            
            #line 26 "E:\MyWork\VeldridGlTF\src\VeldridGlTF.Viewer\Systems\Render\Shaders\PBR\FragmentShader.tt"

	for (int location=0; location<Context.Varyings.Count; ++location)
	{
		WriteLine(string.Format("layout(location = {0}) in {1} {2};", Context.Varyings[location].Location, Glsl.NameOf(Context.Varyings[location].Format), Context.Varyings[location].Name));
	}

            
            #line default
            #line hidden
            this.Write("layout(set = 0, binding = 0) uniform EnvironmentProperties\r\n{\r\n");
            
            #line 34 "E:\MyWork\VeldridGlTF\src\VeldridGlTF.Viewer\Systems\Render\Shaders\PBR\FragmentShader.tt"

	WriteMembers<EnvironmentProperties>();

            
            #line default
            #line hidden
            this.Write("};\r\nlayout (location=0) out vec4 outFragColor;\r\n\r\n#ifdef USE_TEX_LOD\r\n#extension " +
                    "GL_EXT_shader_texture_lod: enable\r\n#endif\r\n\r\n#extension GL_OES_standard_derivati" +
                    "ves : enable\r\n\r\n#ifdef USE_HDR\r\n#extension GL_OES_texture_float : enable\r\n#exten" +
                    "sion GL_OES_texture_float_linear : enable\r\n#endif\r\n\r\nprecision highp float;\r\n\r\nc" +
                    "onst float GAMMA = 2.2;\r\nconst float INV_GAMMA = 1.0 / GAMMA;\r\n\r\n// linear to sR" +
                    "GB approximation\r\n// see http://chilliant.blogspot.com/2012/08/srgb-approximatio" +
                    "ns-for-hlsl.html\r\nvec3 LINEARtoSRGB(vec3 color)\r\n{\r\n    return pow(color, vec3(I" +
                    "NV_GAMMA));\r\n}\r\n\r\n// sRGB to linear approximation\r\n// see http://chilliant.blogs" +
                    "pot.com/2012/08/srgb-approximations-for-hlsl.html\r\nvec4 SRGBtoLINEAR(vec4 srgbIn" +
                    ")\r\n{\r\n    return vec4(pow(srgbIn.xyz, vec3(GAMMA)), srgbIn.w);\r\n}\r\n\r\n// Uncharte" +
                    "d 2 tone map\r\n// see: http://filmicworlds.com/blog/filmic-tonemapping-operators/" +
                    "\r\nvec3 toneMapUncharted2Impl(vec3 color)\r\n{\r\n    const float A = 0.15;\r\n    cons" +
                    "t float B = 0.50;\r\n    const float C = 0.10;\r\n    const float D = 0.20;\r\n    con" +
                    "st float E = 0.02;\r\n    const float F = 0.30;\r\n    return ((color*(A*color+C*B)+" +
                    "D*E)/(color*(A*color+B)+D*F))-E/F;\r\n}\r\n\r\nvec3 toneMapUncharted(vec3 color)\r\n{\r\n " +
                    "   const float W = 11.2;\r\n    color = toneMapUncharted2Impl(color * 2.0);\r\n    v" +
                    "ec3 whiteScale = 1.0 / toneMapUncharted2Impl(vec3(W));\r\n    return LINEARtoSRGB(" +
                    "color * whiteScale);\r\n}\r\n\r\n// Hejl Richard tone map\r\n// see: http://filmicworlds" +
                    ".com/blog/filmic-tonemapping-operators/\r\nvec3 toneMapHejlRichard(vec3 color)\r\n{\r" +
                    "\n    color = max(vec3(0.0), color - vec3(0.004));\r\n    return (color*(6.2*color+" +
                    ".5))/(color*(6.2*color+1.7)+0.06);\r\n}\r\n\r\n// ACES tone map\r\n// see: https://knark" +
                    "owicz.wordpress.com/2016/01/06/aces-filmic-tone-mapping-curve/\r\nvec3 toneMapACES" +
                    "(vec3 color)\r\n{\r\n    const float A = 2.51;\r\n    const float B = 0.03;\r\n    const" +
                    " float C = 2.43;\r\n    const float D = 0.59;\r\n    const float E = 0.14;\r\n    retu" +
                    "rn LINEARtoSRGB(clamp((color * (A * color + B)) / (color * (C * color + D) + E)," +
                    " 0.0, 1.0));\r\n}\r\n\r\nvec3 toneMap(vec3 color)\r\n{\r\n    color *= u_Exposure;\r\n\r\n#ifd" +
                    "ef TONEMAP_UNCHARTED\r\n    return toneMapUncharted(color);\r\n#endif\r\n\r\n#ifdef TONE" +
                    "MAP_HEJLRICHARD\r\n    return toneMapHejlRichard(color);\r\n#endif\r\n\r\n#ifdef TONEMAP" +
                    "_ACES\r\n    return toneMapACES(color);\r\n#endif\r\n\r\n    return LINEARtoSRGB(color);" +
                    "\r\n}\r\n\r\n\r\n// General Material\r\n#ifdef HAS_NORMAL_MAP\r\nuniform sampler2D u_NormalS" +
                    "ampler;\r\nuniform float u_NormalScale;\r\nuniform int u_NormalUVSet;\r\nuniform mat3 " +
                    "u_NormalUVTransform;\r\n#endif\r\n\r\n#ifdef HAS_EMISSIVE_MAP\r\nuniform sampler2D u_Emi" +
                    "ssiveSampler;\r\nuniform int u_EmissiveUVSet;\r\nuniform vec3 u_EmissiveFactor;\r\nuni" +
                    "form mat3 u_EmissiveUVTransform;\r\n#endif\r\n\r\n#ifdef HAS_OCCLUSION_MAP\r\nuniform sa" +
                    "mpler2D u_OcclusionSampler;\r\nuniform int u_OcclusionUVSet;\r\nuniform float u_Occl" +
                    "usionStrength;\r\nuniform mat3 u_OcclusionUVTransform;\r\n#endif\r\n\r\n// Metallic Roug" +
                    "hness Material\r\n#ifdef HAS_BASE_COLOR_MAP\r\nuniform sampler2D u_BaseColorSampler;" +
                    "\r\nuniform int u_BaseColorUVSet;\r\nuniform mat3 u_BaseColorUVTransform;\r\n#endif\r\n\r" +
                    "\n#ifdef HAS_METALLIC_ROUGHNESS_MAP\r\nuniform sampler2D u_MetallicRoughnessSampler" +
                    ";\r\nuniform int u_MetallicRoughnessUVSet;\r\nuniform mat3 u_MetallicRoughnessUVTran" +
                    "sform;\r\n#endif\r\n\r\n// Specular Glossiness Material\r\n#ifdef HAS_DIFFUSE_MAP\r\nunifo" +
                    "rm sampler2D u_DiffuseSampler;\r\nuniform int u_DiffuseUVSet;\r\nuniform mat3 u_Diff" +
                    "useUVTransform;\r\n#endif\r\n\r\n#ifdef HAS_SPECULAR_GLOSSINESS_MAP\r\nuniform sampler2D" +
                    " u_SpecularGlossinessSampler;\r\nuniform int u_SpecularGlossinessUVSet;\r\nuniform m" +
                    "at3 u_SpecularGlossinessUVTransform;\r\n#endif\r\n\r\n// IBL\r\n#ifdef USE_IBL\r\nuniform " +
                    "samplerCube u_DiffuseEnvSampler;\r\nuniform samplerCube u_SpecularEnvSampler;\r\nuni" +
                    "form sampler2D u_brdfLUT;\r\n#endif\r\n\r\nvec2 getNormalUV()\r\n{\r\n    vec3 uv = vec3(v" +
                    "_UVCoord1, 1.0);\r\n#ifdef HAS_NORMAL_MAP\r\n    uv.xy = u_NormalUVSet < 1 ? v_UVCoo" +
                    "rd1 : v_UVCoord2;\r\n    #ifdef HAS_NORMAL_UV_TRANSFORM\r\n    uv *= u_NormalUVTrans" +
                    "form;\r\n    #endif\r\n#endif\r\n    return uv.xy;\r\n}\r\n\r\nvec2 getEmissiveUV()\r\n{\r\n    " +
                    "vec3 uv = vec3(v_UVCoord1, 1.0);\r\n#ifdef HAS_EMISSIVE_MAP\r\n    uv.xy = u_Emissiv" +
                    "eUVSet < 1 ? v_UVCoord1 : v_UVCoord2;\r\n    #ifdef HAS_EMISSIVE_UV_TRANSFORM\r\n   " +
                    " uv *= u_EmissiveUVTransform;\r\n    #endif\r\n#endif\r\n\r\n    return uv.xy;\r\n}\r\n\r\nvec" +
                    "2 getOcclusionUV()\r\n{\r\n    vec3 uv = vec3(v_UVCoord1, 1.0);\r\n#ifdef HAS_OCCLUSIO" +
                    "N_MAP\r\n    uv.xy = u_OcclusionUVSet < 1 ? v_UVCoord1 : v_UVCoord2;\r\n    #ifdef H" +
                    "AS_OCCLSION_UV_TRANSFORM\r\n    uv *= u_OcclusionUVTransform;\r\n    #endif\r\n#endif\r" +
                    "\n    return uv.xy;\r\n}\r\n\r\nvec2 getBaseColorUV()\r\n{\r\n    vec3 uv = vec3(v_UVCoord1" +
                    ", 1.0);\r\n#ifdef HAS_BASE_COLOR_MAP\r\n    uv.xy = u_BaseColorUVSet < 1 ? v_UVCoord" +
                    "1 : v_UVCoord2;\r\n    #ifdef HAS_BASECOLOR_UV_TRANSFORM\r\n    uv *= u_BaseColorUVT" +
                    "ransform;\r\n    #endif\r\n#endif\r\n    return uv.xy;\r\n}\r\n\r\nvec2 getMetallicRoughness" +
                    "UV()\r\n{\r\n    vec3 uv = vec3(v_UVCoord1, 1.0);\r\n#ifdef HAS_METALLIC_ROUGHNESS_MAP" +
                    "\r\n    uv.xy = u_MetallicRoughnessUVSet < 1 ? v_UVCoord1 : v_UVCoord2;\r\n    #ifde" +
                    "f HAS_METALLICROUGHNESS_UV_TRANSFORM\r\n    uv *= u_MetallicRoughnessUVTransform;\r" +
                    "\n    #endif\r\n#endif\r\n    return uv.xy;\r\n}\r\n\r\nvec2 getSpecularGlossinessUV()\r\n{\r\n" +
                    "    vec3 uv = vec3(v_UVCoord1, 1.0);\r\n#ifdef HAS_SPECULAR_GLOSSINESS_MAP\r\n    uv" +
                    ".xy = u_SpecularGlossinessUVSet < 1 ? v_UVCoord1 : v_UVCoord2;\r\n    #ifdef HAS_S" +
                    "PECULARGLOSSINESS_UV_TRANSFORM\r\n    uv *= u_SpecularGlossinessUVTransform;\r\n    " +
                    "#endif\r\n#endif\r\n    return uv.xy;\r\n}\r\n\r\nvec2 getDiffuseUV()\r\n{\r\n    vec3 uv = ve" +
                    "c3(v_UVCoord1, 1.0);\r\n#ifdef HAS_DIFFUSE_MAP\r\n    uv.xy = u_DiffuseUVSet < 1 ? v" +
                    "_UVCoord1 : v_UVCoord2;\r\n    #ifdef HAS_DIFFUSE_UV_TRANSFORM\r\n    uv *= u_Diffus" +
                    "eUVTransform;\r\n    #endif\r\n#endif\r\n    return uv.xy;\r\n}\r\n\r\nconst float M_PI = 3." +
                    "141592653589793;\r\nconst float c_MinReflectance = 0.04;\r\n\r\nstruct AngularInfo\r\n{\r" +
                    "\n    float NdotL;                  // cos angle between normal and light directi" +
                    "on\r\n    float NdotV;                  // cos angle between normal and view direc" +
                    "tion\r\n    float NdotH;                  // cos angle between normal and half vec" +
                    "tor\r\n    float LdotH;                  // cos angle between light direction and " +
                    "half vector\r\n\r\n    float VdotH;                  // cos angle between view direc" +
                    "tion and half vector\r\n\r\n    vec3 padding;\r\n};\r\n\r\nvec4 getVertexColor()\r\n{\r\n   ve" +
                    "c4 color = vec4(1.0, 1.0, 1.0, 1.0);\r\n\r\n#ifdef HAS_VERTEX_COLOR_VEC3\r\n    color." +
                    "rgb = v_Color;\r\n#endif\r\n#ifdef HAS_VERTEX_COLOR_VEC4\r\n    color = v_Color;\r\n#end" +
                    "if\r\n\r\n   return color;\r\n}\r\n\r\n// Find the normal for this fragment, pulling eithe" +
                    "r from a predefined normal map\r\n// or from the interpolated mesh normal and tang" +
                    "ent attributes.\r\nvec3 getNormal()\r\n{\r\n    vec2 UV = getNormalUV();\r\n\r\n    // Ret" +
                    "rieve the tangent space matrix\r\n#ifndef HAS_TANGENTS\r\n    vec3 pos_dx = dFdx(v_P" +
                    "osition);\r\n    vec3 pos_dy = dFdy(v_Position);\r\n    vec3 tex_dx = dFdx(vec3(UV, " +
                    "0.0));\r\n    vec3 tex_dy = dFdy(vec3(UV, 0.0));\r\n    vec3 t = (tex_dy.t * pos_dx " +
                    "- tex_dx.t * pos_dy) / (tex_dx.s * tex_dy.t - tex_dy.s * tex_dx.t);\r\n\r\n#ifdef HA" +
                    "S_NORMALS\r\n    vec3 ng = normalize(v_Normal);\r\n#else\r\n    vec3 ng = cross(pos_dx" +
                    ", pos_dy);\r\n#endif\r\n\r\n    t = normalize(t - ng * dot(ng, t));\r\n    vec3 b = norm" +
                    "alize(cross(ng, t));\r\n    mat3 tbn = mat3(t, b, ng);\r\n#else // HAS_TANGENTS\r\n   " +
                    " mat3 tbn = v_TBN;\r\n#endif\r\n\r\n#ifdef HAS_NORMAL_MAP\r\n    vec3 n = texture2D(u_No" +
                    "rmalSampler, UV).rgb;\r\n    n = normalize(tbn * ((2.0 * n - 1.0) * vec3(u_NormalS" +
                    "cale, u_NormalScale, 1.0)));\r\n#else\r\n    // The tbn matrix is linearly interpola" +
                    "ted, so we need to re-normalize\r\n    vec3 n = normalize(tbn[2].xyz);\r\n#endif\r\n\r\n" +
                    "    return n;\r\n}\r\n\r\nfloat getPerceivedBrightness(vec3 vector)\r\n{\r\n    return sqr" +
                    "t(0.299 * vector.r * vector.r + 0.587 * vector.g * vector.g + 0.114 * vector.b *" +
                    " vector.b);\r\n}\r\n\r\n// https://github.com/KhronosGroup/glTF/blob/master/extensions" +
                    "/2.0/Khronos/KHR_materials_pbrSpecularGlossiness/examples/convert-between-workfl" +
                    "ows/js/three.pbrUtilities.js#L34\r\nfloat solveMetallic(vec3 diffuse, vec3 specula" +
                    "r, float oneMinusSpecularStrength) {\r\n    float specularBrightness = getPerceive" +
                    "dBrightness(specular);\r\n\r\n    if (specularBrightness < c_MinReflectance) {\r\n    " +
                    "    return 0.0;\r\n    }\r\n\r\n    float diffuseBrightness = getPerceivedBrightness(d" +
                    "iffuse);\r\n\r\n    float a = c_MinReflectance;\r\n    float b = diffuseBrightness * o" +
                    "neMinusSpecularStrength / (1.0 - c_MinReflectance) + specularBrightness - 2.0 * " +
                    "c_MinReflectance;\r\n    float c = c_MinReflectance - specularBrightness;\r\n    flo" +
                    "at D = b * b - 4.0 * a * c;\r\n\r\n    return clamp((-b + sqrt(D)) / (2.0 * a), 0.0," +
                    " 1.0);\r\n}\r\n\r\nAngularInfo getAngularInfo(vec3 pointToLight, vec3 normal, vec3 vie" +
                    "w)\r\n{\r\n    // Standard one-letter names\r\n    vec3 n = normalize(normal);        " +
                    "   // Outward direction of surface point\r\n    vec3 v = normalize(view);         " +
                    "    // Direction from surface point to view\r\n    vec3 l = normalize(pointToLight" +
                    ");     // Direction from surface point to light\r\n    vec3 h = normalize(l + v); " +
                    "           // Direction of the vector between l and v\r\n\r\n    float NdotL = clamp" +
                    "(dot(n, l), 0.0, 1.0);\r\n    float NdotV = clamp(dot(n, v), 0.0, 1.0);\r\n    float" +
                    " NdotH = clamp(dot(n, h), 0.0, 1.0);\r\n    float LdotH = clamp(dot(l, h), 0.0, 1." +
                    "0);\r\n    float VdotH = clamp(dot(v, h), 0.0, 1.0);\r\n\r\n    return AngularInfo(\r\n " +
                    "       NdotL,\r\n        NdotV,\r\n        NdotH,\r\n        LdotH,\r\n        VdotH,\r\n " +
                    "       vec3(0, 0, 0)\r\n    );\r\n}\r\n\r\n// KHR_lights_punctual extension.\r\n// see htt" +
                    "ps://github.com/KhronosGroup/glTF/tree/master/extensions/2.0/Khronos/KHR_lights_" +
                    "punctual\r\n\r\nstruct Light\r\n{\r\n    vec3 direction;\r\n    float range;\r\n\r\n    vec3 c" +
                    "olor;\r\n    float intensity;\r\n\r\n    vec3 position;\r\n    float innerConeCos;\r\n\r\n  " +
                    "  float outerConeCos;\r\n    int type;\r\n\r\n    vec2 padding;\r\n};\r\n\r\nconst int Light" +
                    "Type_Directional = 0;\r\nconst int LightType_Point = 1;\r\nconst int LightType_Spot " +
                    "= 2;\r\n\r\n#ifdef USE_PUNCTUAL\r\nuniform Light u_Lights[LIGHT_COUNT];\r\n#endif\r\n\r\n#if" +
                    " defined(MATERIAL_SPECULARGLOSSINESS) || defined(MATERIAL_METALLICROUGHNESS)\r\nun" +
                    "iform float u_MetallicFactor;\r\nuniform float u_RoughnessFactor;\r\nuniform vec4 u_" +
                    "BaseColorFactor;\r\n#endif\r\n\r\n#ifdef MATERIAL_SPECULARGLOSSINESS\r\nuniform vec3 u_S" +
                    "pecularFactor;\r\nuniform vec4 u_DiffuseFactor;\r\nuniform float u_GlossinessFactor;" +
                    "\r\n#endif\r\n\r\n#ifdef ALPHAMODE_MASK\r\nuniform float u_AlphaCutoff;\r\n#endif\r\n\r\nstruc" +
                    "t MaterialInfo\r\n{\r\n    float perceptualRoughness;    // roughness value, as auth" +
                    "ored by the model creator (input to shader)\r\n    vec3 reflectance0;            /" +
                    "/ full reflectance color (normal incidence angle)\r\n\r\n    float alphaRoughness;  " +
                    "       // roughness mapped to a more linear change in the roughness (proposed by" +
                    " [2])\r\n    vec3 diffuseColor;            // color contribution from diffuse ligh" +
                    "ting\r\n\r\n    vec3 reflectance90;           // reflectance color at grazing angle\r" +
                    "\n    vec3 specularColor;           // color contribution from specular lighting\r" +
                    "\n};\r\n\r\n// Calculation of the lighting contribution from an optional Image Based " +
                    "Light source.\r\n// Precomputed Environment Maps are required uniform inputs and a" +
                    "re computed as outlined in [1].\r\n// See our README.md on Environment Maps [3] fo" +
                    "r additional discussion.\r\n#ifdef USE_IBL\r\nvec3 getIBLContribution(MaterialInfo m" +
                    "aterialInfo, vec3 n, vec3 v)\r\n{\r\n    float NdotV = clamp(dot(n, v), 0.0, 1.0);\r\n" +
                    "\r\n    float lod = clamp(materialInfo.perceptualRoughness * float(u_MipCount), 0." +
                    "0, float(u_MipCount));\r\n    vec3 reflection = normalize(reflect(-v, n));\r\n\r\n    " +
                    "vec2 brdfSamplePoint = clamp(vec2(NdotV, materialInfo.perceptualRoughness), vec2" +
                    "(0.0, 0.0), vec2(1.0, 1.0));\r\n    // retrieve a scale and bias to F0. See [1], F" +
                    "igure 3\r\n    vec2 brdf = texture2D(u_brdfLUT, brdfSamplePoint).rg;\r\n\r\n    vec4 d" +
                    "iffuseSample = textureCube(u_DiffuseEnvSampler, n);\r\n\r\n#ifdef USE_TEX_LOD\r\n    v" +
                    "ec4 specularSample = textureCubeLodEXT(u_SpecularEnvSampler, reflection, lod);\r\n" +
                    "#else\r\n    vec4 specularSample = textureCube(u_SpecularEnvSampler, reflection);\r" +
                    "\n#endif\r\n\r\n#ifdef USE_HDR\r\n    // Already linear.\r\n    vec3 diffuseLight = diffu" +
                    "seSample.rgb;\r\n    vec3 specularLight = specularSample.rgb;\r\n#else\r\n    vec3 dif" +
                    "fuseLight = SRGBtoLINEAR(diffuseSample).rgb;\r\n    vec3 specularLight = SRGBtoLIN" +
                    "EAR(specularSample).rgb;\r\n#endif\r\n\r\n    vec3 diffuse = diffuseLight * materialIn" +
                    "fo.diffuseColor;\r\n    vec3 specular = specularLight * (materialInfo.specularColo" +
                    "r * brdf.x + brdf.y);\r\n\r\n    return diffuse + specular;\r\n}\r\n#endif\r\n\r\n// Lambert" +
                    " lighting\r\n// see https://seblagarde.wordpress.com/2012/01/08/pi-or-not-to-pi-in" +
                    "-game-lighting-equation/\r\nvec3 diffuse(MaterialInfo materialInfo)\r\n{\r\n    return" +
                    " materialInfo.diffuseColor / M_PI;\r\n}\r\n\r\n// The following equation models the Fr" +
                    "esnel reflectance term of the spec equation (aka F())\r\n// Implementation of fres" +
                    "nel from [4], Equation 15\r\nvec3 specularReflection(MaterialInfo materialInfo, An" +
                    "gularInfo angularInfo)\r\n{\r\n    return materialInfo.reflectance0 + (materialInfo." +
                    "reflectance90 - materialInfo.reflectance0) * pow(clamp(1.0 - angularInfo.VdotH, " +
                    "0.0, 1.0), 5.0);\r\n}\r\n\r\n// Smith Joint GGX\r\n// Note: Vis = G / (4 * NdotL * NdotV" +
                    ")\r\n// see Eric Heitz. 2014. Understanding the Masking-Shadowing Function in Micr" +
                    "ofacet-Based BRDFs. Journal of Computer Graphics Techniques, 3\r\n// see Real-Time" +
                    " Rendering. Page 331 to 336.\r\n// see https://google.github.io/filament/Filament." +
                    "md.html#materialsystem/specularbrdf/geometricshadowing(specularg)\r\nfloat visibil" +
                    "ityOcclusion(MaterialInfo materialInfo, AngularInfo angularInfo)\r\n{\r\n    float N" +
                    "dotL = angularInfo.NdotL;\r\n    float NdotV = angularInfo.NdotV;\r\n    float alpha" +
                    "RoughnessSq = materialInfo.alphaRoughness * materialInfo.alphaRoughness;\r\n\r\n    " +
                    "float GGXV = NdotL * sqrt(NdotV * NdotV * (1.0 - alphaRoughnessSq) + alphaRoughn" +
                    "essSq);\r\n    float GGXL = NdotV * sqrt(NdotL * NdotL * (1.0 - alphaRoughnessSq) " +
                    "+ alphaRoughnessSq);\r\n\r\n    float GGX = GGXV + GGXL;\r\n    if (GGX > 0.0)\r\n    {\r" +
                    "\n        return 0.5 / GGX;\r\n    }\r\n    return 0.0;\r\n}\r\n\r\n// The following equati" +
                    "on(s) model the distribution of microfacet normals across the area being drawn (" +
                    "aka D())\r\n// Implementation from \"Average Irregularity Representation of a Rough" +
                    "ened Surface for Ray Reflection\" by T. S. Trowbridge, and K. P. Reitz\r\n// Follow" +
                    "s the distribution function recommended in the SIGGRAPH 2013 course notes from E" +
                    "PIC Games [1], Equation 3.\r\nfloat microfacetDistribution(MaterialInfo materialIn" +
                    "fo, AngularInfo angularInfo)\r\n{\r\n    float alphaRoughnessSq = materialInfo.alpha" +
                    "Roughness * materialInfo.alphaRoughness;\r\n    float f = (angularInfo.NdotH * alp" +
                    "haRoughnessSq - angularInfo.NdotH) * angularInfo.NdotH + 1.0;\r\n    return alphaR" +
                    "oughnessSq / (M_PI * f * f);\r\n}\r\n\r\nvec3 getPointShade(vec3 pointToLight, Materia" +
                    "lInfo materialInfo, vec3 normal, vec3 view)\r\n{\r\n    AngularInfo angularInfo = ge" +
                    "tAngularInfo(pointToLight, normal, view);\r\n\r\n    if (angularInfo.NdotL > 0.0 || " +
                    "angularInfo.NdotV > 0.0)\r\n    {\r\n        // Calculate the shading terms for the " +
                    "microfacet specular shading model\r\n        vec3 F = specularReflection(materialI" +
                    "nfo, angularInfo);\r\n        float Vis = visibilityOcclusion(materialInfo, angula" +
                    "rInfo);\r\n        float D = microfacetDistribution(materialInfo, angularInfo);\r\n\r" +
                    "\n        // Calculation of analytical lighting contribution\r\n        vec3 diffus" +
                    "eContrib = (1.0 - F) * diffuse(materialInfo);\r\n        vec3 specContrib = F * Vi" +
                    "s * D;\r\n\r\n        // Obtain final intensity as reflectance (BRDF) scaled by the " +
                    "energy of the light (cosine law)\r\n        return angularInfo.NdotL * (diffuseCon" +
                    "trib + specContrib);\r\n    }\r\n\r\n    return vec3(0.0, 0.0, 0.0);\r\n}\r\n\r\n// https://" +
                    "github.com/KhronosGroup/glTF/blob/master/extensions/2.0/Khronos/KHR_lights_punct" +
                    "ual/README.md#range-property\r\nfloat getRangeAttenuation(float range, float dista" +
                    "nce)\r\n{\r\n    if (range < 0.0)\r\n    {\r\n        // negative range means unlimited\r" +
                    "\n        return 1.0;\r\n    }\r\n    return max(min(1.0 - pow(distance / range, 4.0)" +
                    ", 1.0), 0.0) / pow(distance, 2.0);\r\n}\r\n\r\n// https://github.com/KhronosGroup/glTF" +
                    "/blob/master/extensions/2.0/Khronos/KHR_lights_punctual/README.md#inner-and-oute" +
                    "r-cone-angles\r\nfloat getSpotAttenuation(vec3 pointToLight, vec3 spotDirection, f" +
                    "loat outerConeCos, float innerConeCos)\r\n{\r\n    float actualCos = dot(normalize(s" +
                    "potDirection), normalize(-pointToLight));\r\n    if (actualCos > outerConeCos)\r\n  " +
                    "  {\r\n        if (actualCos < innerConeCos)\r\n        {\r\n            return smooth" +
                    "step(outerConeCos, innerConeCos, actualCos);\r\n        }\r\n        return 1.0;\r\n  " +
                    "  }\r\n    return 0.0;\r\n}\r\n\r\nvec3 applyDirectionalLight(Light light, MaterialInfo " +
                    "materialInfo, vec3 normal, vec3 view)\r\n{\r\n    vec3 pointToLight = -light.directi" +
                    "on;\r\n    vec3 shade = getPointShade(pointToLight, materialInfo, normal, view);\r\n" +
                    "    return light.intensity * light.color * shade;\r\n}\r\n\r\nvec3 applyPointLight(Lig" +
                    "ht light, MaterialInfo materialInfo, vec3 normal, vec3 view)\r\n{\r\n    vec3 pointT" +
                    "oLight = light.position - v_Position;\r\n    float distance = length(pointToLight)" +
                    ";\r\n    float attenuation = getRangeAttenuation(light.range, distance);\r\n    vec3" +
                    " shade = getPointShade(pointToLight, materialInfo, normal, view);\r\n    return at" +
                    "tenuation * light.intensity * light.color * shade;\r\n}\r\n\r\nvec3 applySpotLight(Lig" +
                    "ht light, MaterialInfo materialInfo, vec3 normal, vec3 view)\r\n{\r\n    vec3 pointT" +
                    "oLight = light.position - v_Position;\r\n    float distance = length(pointToLight)" +
                    ";\r\n    float rangeAttenuation = getRangeAttenuation(light.range, distance);\r\n   " +
                    " float spotAttenuation = getSpotAttenuation(pointToLight, light.direction, light" +
                    ".outerConeCos, light.innerConeCos);\r\n    vec3 shade = getPointShade(pointToLight" +
                    ", materialInfo, normal, view);\r\n    return rangeAttenuation * spotAttenuation * " +
                    "light.intensity * light.color * shade;\r\n}\r\n\r\nvoid main()\r\n{\r\n    // Metallic and" +
                    " Roughness material properties are packed together\r\n    // In glTF, these factor" +
                    "s can be specified by fixed scalar values\r\n    // or from a metallic-roughness m" +
                    "ap\r\n    float perceptualRoughness = 0.0;\r\n    float metallic = 0.0;\r\n    vec4 ba" +
                    "seColor = vec4(0.0, 0.0, 0.0, 1.0);\r\n    vec3 diffuseColor = vec3(0.0);\r\n    vec" +
                    "3 specularColor= vec3(0.0);\r\n    vec3 f0 = vec3(0.04);\r\n\r\n#ifdef MATERIAL_SPECUL" +
                    "ARGLOSSINESS\r\n\r\n#ifdef HAS_SPECULAR_GLOSSINESS_MAP\r\n    vec4 sgSample = SRGBtoLI" +
                    "NEAR(texture2D(u_SpecularGlossinessSampler, getSpecularGlossinessUV()));\r\n    pe" +
                    "rceptualRoughness = (1.0 - sgSample.a * u_GlossinessFactor); // glossiness to ro" +
                    "ughness\r\n    f0 = sgSample.rgb * u_SpecularFactor; // specular\r\n#else\r\n    f0 = " +
                    "u_SpecularFactor;\r\n    perceptualRoughness = 1.0 - u_GlossinessFactor;\r\n#endif /" +
                    "/ ! HAS_SPECULAR_GLOSSINESS_MAP\r\n\r\n#ifdef HAS_DIFFUSE_MAP\r\n    baseColor = SRGBt" +
                    "oLINEAR(texture2D(u_DiffuseSampler, getDiffuseUV())) * u_DiffuseFactor;\r\n#else\r\n" +
                    "    baseColor = u_DiffuseFactor;\r\n#endif // !HAS_DIFFUSE_MAP\r\n\r\n    baseColor *=" +
                    " getVertexColor();\r\n\r\n    // f0 = specular\r\n    specularColor = f0;\r\n    float o" +
                    "neMinusSpecularStrength = 1.0 - max(max(f0.r, f0.g), f0.b);\r\n    diffuseColor = " +
                    "baseColor.rgb * oneMinusSpecularStrength;\r\n\r\n#ifdef DEBUG_METALLIC\r\n    // do co" +
                    "nversion between metallic M-R and S-G metallic\r\n    metallic = solveMetallic(bas" +
                    "eColor.rgb, specularColor, oneMinusSpecularStrength);\r\n#endif // ! DEBUG_METALLI" +
                    "C\r\n\r\n#endif // ! MATERIAL_SPECULARGLOSSINESS\r\n\r\n#ifdef MATERIAL_METALLICROUGHNES" +
                    "S\r\n\r\n#ifdef HAS_METALLIC_ROUGHNESS_MAP\r\n    // Roughness is stored in the \'g\' ch" +
                    "annel, metallic is stored in the \'b\' channel.\r\n    // This layout intentionally " +
                    "reserves the \'r\' channel for (optional) occlusion map data\r\n    vec4 mrSample = " +
                    "texture2D(u_MetallicRoughnessSampler, getMetallicRoughnessUV());\r\n    perceptual" +
                    "Roughness = mrSample.g * u_RoughnessFactor;\r\n    metallic = mrSample.b * u_Metal" +
                    "licFactor;\r\n#else\r\n    metallic = u_MetallicFactor;\r\n    perceptualRoughness = u" +
                    "_RoughnessFactor;\r\n#endif\r\n\r\n    // The albedo may be defined from a base textur" +
                    "e or a flat color\r\n#ifdef HAS_BASE_COLOR_MAP\r\n    baseColor = SRGBtoLINEAR(textu" +
                    "re2D(u_BaseColorSampler, getBaseColorUV())) * u_BaseColorFactor;\r\n#else\r\n    bas" +
                    "eColor = u_BaseColorFactor;\r\n#endif\r\n\r\n    baseColor *= getVertexColor();\r\n\r\n   " +
                    " diffuseColor = baseColor.rgb * (vec3(1.0) - f0) * (1.0 - metallic);\r\n\r\n    spec" +
                    "ularColor = mix(f0, baseColor.rgb, metallic);\r\n\r\n#endif // ! MATERIAL_METALLICRO" +
                    "UGHNESS\r\n\r\n#ifdef ALPHAMODE_MASK\r\n    if(baseColor.a < u_AlphaCutoff)\r\n    {\r\n  " +
                    "      discard;\r\n    }\r\n    baseColor.a = 1.0;\r\n#endif\r\n\r\n#ifdef ALPHAMODE_OPAQUE" +
                    "\r\n    baseColor.a = 1.0;\r\n#endif\r\n\r\n#ifdef MATERIAL_UNLIT\r\n    outFragColor = ve" +
                    "c4(LINEARtoSRGB(baseColor.rgb), baseColor.a);\r\n    return;\r\n#endif\r\n\r\n    percep" +
                    "tualRoughness = clamp(perceptualRoughness, 0.0, 1.0);\r\n    metallic = clamp(meta" +
                    "llic, 0.0, 1.0);\r\n\r\n    // Roughness is authored as perceptual roughness; as is " +
                    "convention,\r\n    // convert to material roughness by squaring the perceptual rou" +
                    "ghness [2].\r\n    float alphaRoughness = perceptualRoughness * perceptualRoughnes" +
                    "s;\r\n\r\n    // Compute reflectance.\r\n    float reflectance = max(max(specularColor" +
                    ".r, specularColor.g), specularColor.b);\r\n\r\n    vec3 specularEnvironmentR0 = spec" +
                    "ularColor.rgb;\r\n    // Anything less than 2% is physically impossible and is ins" +
                    "tead considered to be shadowing. Compare to \"Real-Time-Rendering\" 4th editon on " +
                    "page 325.\r\n    vec3 specularEnvironmentR90 = vec3(clamp(reflectance * 50.0, 0.0," +
                    " 1.0));\r\n\r\n    MaterialInfo materialInfo = MaterialInfo(\r\n        perceptualRoug" +
                    "hness,\r\n        specularEnvironmentR0,\r\n        alphaRoughness,\r\n        diffuse" +
                    "Color,\r\n        specularEnvironmentR90,\r\n        specularColor\r\n    );\r\n\r\n    //" +
                    " LIGHTING\r\n\r\n    vec3 color = vec3(0.0, 0.0, 0.0);\r\n    vec3 normal = getNormal(" +
                    ");\r\n    vec3 view = normalize(u_Camera - v_Position);\r\n\r\n#ifdef USE_PUNCTUAL\r\n  " +
                    "  for (int i = 0; i < LIGHT_COUNT; ++i)\r\n    {\r\n        Light light = u_Lights[i" +
                    "];\r\n        if (light.type == LightType_Directional)\r\n        {\r\n            col" +
                    "or += applyDirectionalLight(light, materialInfo, normal, view);\r\n        }\r\n    " +
                    "    else if (light.type == LightType_Point)\r\n        {\r\n            color += app" +
                    "lyPointLight(light, materialInfo, normal, view);\r\n        }\r\n        else if (li" +
                    "ght.type == LightType_Spot)\r\n        {\r\n            color += applySpotLight(ligh" +
                    "t, materialInfo, normal, view);\r\n        }\r\n    }\r\n#endif\r\n\r\n    // Calculate li" +
                    "ghting contribution from image based lighting source (IBL)\r\n#ifdef USE_IBL\r\n    " +
                    "color += getIBLContribution(materialInfo, normal, view);\r\n#endif\r\n\r\n    float ao" +
                    " = 1.0;\r\n    // Apply optional PBR terms for additional (optional) shading\r\n#ifd" +
                    "ef HAS_OCCLUSION_MAP\r\n    ao = texture2D(u_OcclusionSampler,  getOcclusionUV())." +
                    "r;\r\n    color = mix(color, color * ao, u_OcclusionStrength);\r\n#endif\r\n\r\n    vec3" +
                    " emissive = vec3(0);\r\n#ifdef HAS_EMISSIVE_MAP\r\n    emissive = SRGBtoLINEAR(textu" +
                    "re2D(u_EmissiveSampler, getEmissiveUV())).rgb * u_EmissiveFactor;\r\n    color += " +
                    "emissive;\r\n#endif\r\n\r\n#ifndef DEBUG_OUTPUT // no debug\r\n\r\n   // regular shading\r\n" +
                    "    outFragColor = vec4(toneMap(color), baseColor.a);\r\n\r\n#else // debug output\r\n" +
                    "\r\n    #ifdef DEBUG_METALLIC\r\n        outFragColor.rgb = vec3(metallic);\r\n    #en" +
                    "dif\r\n\r\n    #ifdef DEBUG_ROUGHNESS\r\n        outFragColor.rgb = vec3(perceptualRou" +
                    "ghness);\r\n    #endif\r\n\r\n    #ifdef DEBUG_NORMAL\r\n        #ifdef HAS_NORMAL_MAP\r\n" +
                    "            outFragColor.rgb = texture2D(u_NormalSampler, getNormalUV()).rgb;\r\n " +
                    "       #else\r\n            outFragColor.rgb = vec3(0.5, 0.5, 1.0);\r\n        #endi" +
                    "f\r\n    #endif\r\n\r\n    #ifdef DEBUG_BASECOLOR\r\n        outFragColor.rgb = LINEARto" +
                    "SRGB(baseColor.rgb);\r\n    #endif\r\n\r\n    #ifdef DEBUG_OCCLUSION\r\n        outFragC" +
                    "olor.rgb = vec3(ao);\r\n    #endif\r\n\r\n    #ifdef DEBUG_EMISSIVE\r\n        outFragCo" +
                    "lor.rgb = LINEARtoSRGB(emissive);\r\n    #endif\r\n\r\n    #ifdef DEBUG_F0\r\n        ou" +
                    "tFragColor.rgb = vec3(f0);\r\n    #endif\r\n\r\n    #ifdef DEBUG_ALPHA\r\n        outFra" +
                    "gColor.rgb = vec3(baseColor.a);\r\n    #endif\r\n\r\n    outFragColor.a = 1.0;\r\n\r\n#end" +
                    "if // !DEBUG_OUTPUT\r\n}");
            return this.GenerationEnvironment.ToString();
        }
    }
    
    #line default
    #line hidden
    #region Base class
    /// <summary>
    /// Base class for this transformation
    /// </summary>
    [global::System.CodeDom.Compiler.GeneratedCodeAttribute("Microsoft.VisualStudio.TextTemplating", "16.0.0.0")]
    public class FragmentShaderBase
    {
        #region Fields
        private global::System.Text.StringBuilder generationEnvironmentField;
        private global::System.CodeDom.Compiler.CompilerErrorCollection errorsField;
        private global::System.Collections.Generic.List<int> indentLengthsField;
        private string currentIndentField = "";
        private bool endsWithNewline;
        private global::System.Collections.Generic.IDictionary<string, object> sessionField;
        #endregion
        #region Properties
        /// <summary>
        /// The string builder that generation-time code is using to assemble generated output
        /// </summary>
        protected System.Text.StringBuilder GenerationEnvironment
        {
            get
            {
                if ((this.generationEnvironmentField == null))
                {
                    this.generationEnvironmentField = new global::System.Text.StringBuilder();
                }
                return this.generationEnvironmentField;
            }
            set
            {
                this.generationEnvironmentField = value;
            }
        }
        /// <summary>
        /// The error collection for the generation process
        /// </summary>
        public System.CodeDom.Compiler.CompilerErrorCollection Errors
        {
            get
            {
                if ((this.errorsField == null))
                {
                    this.errorsField = new global::System.CodeDom.Compiler.CompilerErrorCollection();
                }
                return this.errorsField;
            }
        }
        /// <summary>
        /// A list of the lengths of each indent that was added with PushIndent
        /// </summary>
        private System.Collections.Generic.List<int> indentLengths
        {
            get
            {
                if ((this.indentLengthsField == null))
                {
                    this.indentLengthsField = new global::System.Collections.Generic.List<int>();
                }
                return this.indentLengthsField;
            }
        }
        /// <summary>
        /// Gets the current indent we use when adding lines to the output
        /// </summary>
        public string CurrentIndent
        {
            get
            {
                return this.currentIndentField;
            }
        }
        /// <summary>
        /// Current transformation session
        /// </summary>
        public virtual global::System.Collections.Generic.IDictionary<string, object> Session
        {
            get
            {
                return this.sessionField;
            }
            set
            {
                this.sessionField = value;
            }
        }
        #endregion
        #region Transform-time helpers
        /// <summary>
        /// Write text directly into the generated output
        /// </summary>
        public void Write(string textToAppend)
        {
            if (string.IsNullOrEmpty(textToAppend))
            {
                return;
            }
            // If we're starting off, or if the previous text ended with a newline,
            // we have to append the current indent first.
            if (((this.GenerationEnvironment.Length == 0) 
                        || this.endsWithNewline))
            {
                this.GenerationEnvironment.Append(this.currentIndentField);
                this.endsWithNewline = false;
            }
            // Check if the current text ends with a newline
            if (textToAppend.EndsWith(global::System.Environment.NewLine, global::System.StringComparison.CurrentCulture))
            {
                this.endsWithNewline = true;
            }
            // This is an optimization. If the current indent is "", then we don't have to do any
            // of the more complex stuff further down.
            if ((this.currentIndentField.Length == 0))
            {
                this.GenerationEnvironment.Append(textToAppend);
                return;
            }
            // Everywhere there is a newline in the text, add an indent after it
            textToAppend = textToAppend.Replace(global::System.Environment.NewLine, (global::System.Environment.NewLine + this.currentIndentField));
            // If the text ends with a newline, then we should strip off the indent added at the very end
            // because the appropriate indent will be added when the next time Write() is called
            if (this.endsWithNewline)
            {
                this.GenerationEnvironment.Append(textToAppend, 0, (textToAppend.Length - this.currentIndentField.Length));
            }
            else
            {
                this.GenerationEnvironment.Append(textToAppend);
            }
        }
        /// <summary>
        /// Write text directly into the generated output
        /// </summary>
        public void WriteLine(string textToAppend)
        {
            this.Write(textToAppend);
            this.GenerationEnvironment.AppendLine();
            this.endsWithNewline = true;
        }
        /// <summary>
        /// Write formatted text directly into the generated output
        /// </summary>
        public void Write(string format, params object[] args)
        {
            this.Write(string.Format(global::System.Globalization.CultureInfo.CurrentCulture, format, args));
        }
        /// <summary>
        /// Write formatted text directly into the generated output
        /// </summary>
        public void WriteLine(string format, params object[] args)
        {
            this.WriteLine(string.Format(global::System.Globalization.CultureInfo.CurrentCulture, format, args));
        }
        /// <summary>
        /// Raise an error
        /// </summary>
        public void Error(string message)
        {
            System.CodeDom.Compiler.CompilerError error = new global::System.CodeDom.Compiler.CompilerError();
            error.ErrorText = message;
            this.Errors.Add(error);
        }
        /// <summary>
        /// Raise a warning
        /// </summary>
        public void Warning(string message)
        {
            System.CodeDom.Compiler.CompilerError error = new global::System.CodeDom.Compiler.CompilerError();
            error.ErrorText = message;
            error.IsWarning = true;
            this.Errors.Add(error);
        }
        /// <summary>
        /// Increase the indent
        /// </summary>
        public void PushIndent(string indent)
        {
            if ((indent == null))
            {
                throw new global::System.ArgumentNullException("indent");
            }
            this.currentIndentField = (this.currentIndentField + indent);
            this.indentLengths.Add(indent.Length);
        }
        /// <summary>
        /// Remove the last indent that was added with PushIndent
        /// </summary>
        public string PopIndent()
        {
            string returnValue = "";
            if ((this.indentLengths.Count > 0))
            {
                int indentLength = this.indentLengths[(this.indentLengths.Count - 1)];
                this.indentLengths.RemoveAt((this.indentLengths.Count - 1));
                if ((indentLength > 0))
                {
                    returnValue = this.currentIndentField.Substring((this.currentIndentField.Length - indentLength));
                    this.currentIndentField = this.currentIndentField.Remove((this.currentIndentField.Length - indentLength));
                }
            }
            return returnValue;
        }
        /// <summary>
        /// Remove any indentation
        /// </summary>
        public void ClearIndent()
        {
            this.indentLengths.Clear();
            this.currentIndentField = "";
        }
        #endregion
        #region ToString Helpers
        /// <summary>
        /// Utility class to produce culture-oriented representation of an object as a string.
        /// </summary>
        public class ToStringInstanceHelper
        {
            private System.IFormatProvider formatProviderField  = global::System.Globalization.CultureInfo.InvariantCulture;
            /// <summary>
            /// Gets or sets format provider to be used by ToStringWithCulture method.
            /// </summary>
            public System.IFormatProvider FormatProvider
            {
                get
                {
                    return this.formatProviderField ;
                }
                set
                {
                    if ((value != null))
                    {
                        this.formatProviderField  = value;
                    }
                }
            }
            /// <summary>
            /// This is called from the compile/run appdomain to convert objects within an expression block to a string
            /// </summary>
            public string ToStringWithCulture(object objectToConvert)
            {
                if ((objectToConvert == null))
                {
                    throw new global::System.ArgumentNullException("objectToConvert");
                }
                System.Type t = objectToConvert.GetType();
                System.Reflection.MethodInfo method = t.GetMethod("ToString", new System.Type[] {
                            typeof(System.IFormatProvider)});
                if ((method == null))
                {
                    return objectToConvert.ToString();
                }
                else
                {
                    return ((string)(method.Invoke(objectToConvert, new object[] {
                                this.formatProviderField })));
                }
            }
        }
        private ToStringInstanceHelper toStringHelperField = new ToStringInstanceHelper();
        /// <summary>
        /// Helper to produce culture-oriented representation of an object as a string
        /// </summary>
        public ToStringInstanceHelper ToStringHelper
        {
            get
            {
                return this.toStringHelperField;
            }
        }
        #endregion
    }
    #endregion
}
